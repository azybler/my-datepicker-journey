import { Column, Row } from '../Column'
import scaleDownDayGrid2 from '/src/assets/scale_down_day_grid2.png'

# Animating the day grid (part 2)

<Row>
  <Column>
    $$
      s_n = \frac{w}{w/c} = c
    $$

    $$ w = $$ rendered width of the calendar, in this case 300
    
    $$ c = $$ number of columns, in this case 4
    
    $$ s_n = $$ the scale we need start the transform from

    ```css
    @keyframes monthGridScaleAnimation {
      0% {
        transform: scale(4);
      }
      100% {
        transform: scale(1);
      }
    }

    .month-grid-animate-contract {
      animation: monthGridScaleAnimation 0.3s ease-in forwards;
    }
    ```
  </Column>
  <Column>
    <img src={scaleDownDayGrid2} width="600" alt="scaling down the day grid" />
  </Column>
</Row>
